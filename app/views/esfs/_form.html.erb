<%= form_for(@esf) do |f| %>
  <% if @esf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@esf.errors.count, "error") %> prohibited this esf from being saved:</h2>

      <ul>
      <% @esf.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
