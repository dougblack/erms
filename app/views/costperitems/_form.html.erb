<%= form_for(@costperitem) do |f| %>
  <% if @costperitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@costperitem.errors.count, "error") %> prohibited this costperitem from being saved:</h2>

      <ul>
      <% @costperitem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item %><br />
    <%= f.text_field :item %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
