<%= form_for(@governmentagency) do |f| %>
  <% if @governmentagency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@governmentagency.errors.count, "error") %> prohibited this governmentagency from being saved:</h2>

      <ul>
      <% @governmentagency.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br />
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :jurisdiction %><br />
    <%= f.text_field :jurisdiction %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
